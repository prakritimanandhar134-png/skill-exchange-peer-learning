function Navbar() {
  return (
    <nav>
      <h2>SkillSync</h2>

      <div>
        <a href="#">Home</a>
        <a href="#">Skills</a>
        <a href="#">Teach</a>
        <a href="#">Login</a>
      </div>
    </nav>
  );
}

export default Navbar;